{
  "name": "example",
  "version": "1.0.0",
  "license": "GPL-V3",
  "private": true,
  "dependencies": {
    "@forestadmin/agent": "^1.0.0",
    "@forestadmin/datasource-dummy": "^1.0.0"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf coverage dist",
    "lint": "prettier -c src test && eslint src test",
    "start": "node -e \"require('./dist/index.js').default()\"",
    "test": "jest"
  },
  "devDependencies": {
    "superagent": "^6.1.0"
  }
}
