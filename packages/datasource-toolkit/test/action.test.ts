import { ActionFieldType, ActionResponseType } from "../src";

describe("definitions", () => {
  describe("ActionFieldType", () => {
    it("should be defined", () => {
      expect(ActionFieldType).toBeDefined();
    });
  });

  describe("ActionResponseType", () => {
    it("should be defined", () => {
      expect(ActionResponseType).toBeDefined();
    });
  });
});
