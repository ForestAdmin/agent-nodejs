{
  "name": "@forestadmin/plugin-import-fields",
  "version": "0.0.0-development",
  "description": "A plugin that import all fields from a relation",
  "main": "dist/index.js",
  "license": "GPL-3.0",
  "files": [
    "dist/src/**/*.js",
    "dist/src/**/*.d.ts"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf coverage dist",
    "lint": "eslint src test",
    "test": "jest"
  },
  "devDependencies": {
    "@forestadmin/datasource-customizer": "^1.2.0",
    "@forestadmin/datasource-toolkit": "^1.0.0"
  }
}
