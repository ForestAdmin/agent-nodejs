{
  "name": "@forestadmin/datasource-sequelize",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "GPL-3.0",
  "dependencies": {
    "@forestadmin/datasource-toolkit": "^1.0.0"
  },
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf coverage dist",
    "lint": "eslint src test",
    "test": "jest"
  },
  "devDependencies_comments": {
    "sequelize": "~6.14.1 => compilation error with 6.15.0, remove constraint when possible"
  },
  "devDependencies": {
    "fishery": "^2.1.0",
    "sequelize": "~6.14.1",
    "sqlite3": "^5.0.2"
  },
  "peerDependencies": {
    "sequelize": ">= 6.14.1"
  }
}
