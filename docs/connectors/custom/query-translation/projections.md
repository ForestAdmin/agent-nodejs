A projection allows to fetch partials records.

For performance reason, Forest Admin only loads the fields which are needed to perform the task at hand.

Projections simply are an array of fields names

```json
["id", "title", "isbn-10"]
```
